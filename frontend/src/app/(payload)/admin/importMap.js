/**
 * Payload admin import map.
 *
 * This file is normally emitted by `payload generate:importmap`. It is empty
 * because the current config registers no custom admin components. After adding
 * any custom field/view components, regenerate it with:
 *
 *   npx payload generate:importmap
 *
 * (and `npx payload generate:types` to refresh src/payload-types.ts).
 */
export const importMap = {};
